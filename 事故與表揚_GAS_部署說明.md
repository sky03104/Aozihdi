# 事故報告／匿名表揚 後端 GAS v3.1 部署說明

**對應檔案**：`事故與表揚_後端_GAS_v3.1.gs`
**目的**：為「事故報告」與「匿名表揚／檢舉」兩分頁新增「狀態」欄位，並提供主管專用畫面所需的「讀清單」「改狀態」端點。
**重點**：GAS 網址不變（前端三檔皆指向同一支 `…AKfycbwHX…/exec`），但**改完一定要重新部署成新版本**，否則新端點不會生效。

---

## 一、為什麼要更新

主管專用畫面（`tool_report.html?mode=admin`、`tool_feedback.html?mode=admin`）與首頁「待審報告／待處理表揚反應」兩格卡片，需要後端提供：

| 端點 | 方法 | 用途 |
|------|------|------|
| `?action=getReports` | GET | 讀事故報告清單（含列號 `row` 與「狀態」） |
| `?action=getFeedback` | GET | 讀匿名表揚／檢舉清單（含列號、後台工號姓名、狀態） |
| `action=updateStatus` | POST | 主管修改某筆的處理狀態 |

舊版 v3.0 沒有這些端點，也沒有「狀態」欄位，故需更新。

---

## 二、部署步驟

1. 打開原本那支 GAS 專案（與目前 `…AKfycbwHX…/exec` 綁定的同一支）。
2. 把專案內**舊的事故報告 / 匿名舉報程式碼全部刪除**（避免出現兩個 `doPost`／`doGet`）。
3. 將 `事故與表揚_後端_GAS_v3.1.gs` 的**全部內容**貼上。
4. 先手動執行一次 `forceAuth()` → 同意 Drive／試算表授權（首次必做）。
5. 可選：執行 `testGetReports()`、`testGetFeedback()` 看 Log 是否正常回傳 JSON。
6. **部署 → 管理部署作業 → 選現有部署 → ✏️ 編輯 → 版本：「新版本」→ 部署**。
   - 執行身分＝我；具有存取權的使用者＝**所有人**。
   - 網址必須維持 `…AKfycbwHX…/exec`（沿用同一部署，網址才不變）。

> ⚠️ 若「建立新部署」而非「編輯既有部署」，會產生**新網址**，前端三檔就對不到。務必用「編輯既有部署 + 新版本」。

---

## 三、資料表變動

- `事故報告` 分頁：末欄新增「狀態」。新報告送出時自動寫入「未讀」。
- `匿名表揚檢舉` 分頁：末欄新增「狀態」。新提交自動寫入「未讀」。
- 既有舊資料：程式會自動補表頭；舊列「狀態」為空者，清單與卡片一律視同「未讀」。
- 主鍵：以試算表**實際列號**為更新依據（append-only 不刪列，列號穩定）；清單另回傳顯示用「編號 = 列號-1」純數字流水號。

**狀態模型（主管畫面打開資料時會自動轉一次）**：
- 事故報告：`未讀` →（主管打開）自動轉 `待處理` →（手動）`待處理`／`已知悉再觀察`／`持續追蹤`／`已處理`。
  - 分頁：未讀／待處理／已處理／全部（「待處理」分頁＝所有處理中狀態：待處理＋已知悉再觀察＋持續追蹤）。
- 表揚／反應：`未讀` →（主管打開）自動轉 `已讀` →（主管裁決）`已處理`，同時把決定寫入**「處置」欄**：
  - 表揚類型按鈕：同意表揚／不同意表揚；反應類型按鈕：同意懲處／不同意懲處。
  - 分頁：未讀／已讀／已處理／全部。
  - 試算表 `匿名表揚檢舉` 分頁**新增「處置」欄**（在「狀態」之後），記錄主管裁決字串。
- GAS 狀態白名單同時保留 `處理中` 以相容舊資料，不影響上述流程。
- 「上一篇／下一篇」依**進入詳情當下的分頁順序凍結**，自動轉狀態不會改變瀏覽順序（例：在「未讀」分頁打開後仍沿未讀清單前後翻）。

---

## 四、前端對應行為

| 前端 | 計數 / 篩選依據 |
|------|----------------|
| 首頁「待審報告」卡片 | 狀態 **≠ 已處理** 的筆數（未讀＋待處理，紅字提醒） |
| 首頁「待處理表揚/反應」卡片 | 狀態 **≠ 已處理** 的筆數（未讀＋已讀，橙字提醒） |
| 事故報告主管畫面分頁 | 未讀／待處理／已處理／全部 |
| 表揚／反應主管畫面分頁 | 未讀／已讀／已處理／全部 |

主管畫面與卡片僅 `executive`（公司主管）／`admin`（管理員）角色可見。

---

## 五、驗證清單

- [ ] `forceAuth()` 執行成功（Drive + Sheet 已授權）。
- [ ] 用填寫模式各送 1 筆 → 試算表新列末欄＝「未讀」。
- [ ] 瀏覽器開 `…/exec?action=getReports` → 回傳 `{"status":"ok","list":[…]}`。
- [ ] 主管畫面清單可見資料；點單筆 → 改狀態 → 重新整理仍保持；試算表對應列同步更新。
- [ ] 首頁以公司主管登入 → 兩格卡片數字＝未處理筆數；點卡片開對應主管畫面。
