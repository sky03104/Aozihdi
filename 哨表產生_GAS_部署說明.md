# 哨表自動產生工具 — GAS 部署說明

**工具**：哨表自動產生（`tool_guard_gen.html`）
**目標試算表**：既有的哨表試算表（`1sIcdAhw0mz5iM3F5fulDNPOda2pv-t7xUhT6XXf9X7Q`，與「哨表上傳_GAS_v6.gs」共用），本檔額外新增一個分頁 `哨點設定`（GAS 第一次使用時自動建立）。

---

## 架構：兩個 GAS 各司其職

哨表自動產生工具同時打兩支 GAS，職責不重疊：

| 連線 | 變數（`tool_guard_gen.html`） | 指向 | 負責 |
|------|------|------|------|
| 新部署 | `GUARD_GEN_GAS_URL` | **本說明要部署的 `哨表產生_GAS.gs`** | 讀/寫「哨點設定」、讀早晚班月班表算出當日在職名單 |
| 沿用既有 | `GD_UPLOAD_GAS_URL` | 既有的「哨表上傳_GAS_v6.gs」（已部署，網址已寫死在檔案裡） | 把算好的 18×14 陣列寫進「今日哨表/明日哨表」分頁＋更新歷史哨表 |

**資料流**：
1. 開「哨點設定」分頁 → 前端讀 `getEmployeeNames`（早/晚班月班表姓名欄去重）+ `getGuardPostConfig`（既有設定）→ 管理員勾選每位員工適任哨點 → `saveGuardPostConfig` 整批覆蓋寫入。
2. 開「產生哨表」分頁 → 選日期 + 4 個增派勾選框 → `getMonthlyRoster` 讀當天早/晚班在職名單（已排除休假代號）→ 前端純函式 `buildGuardMatrix()` 算出 18×14 陣列（隨機分配崗位、巡檢/停巡/泊車增派、見習自動歸位）→ 預覽 → 「產生 xlsx」下載（帶合併儲存格）或「上傳至試算表」（呼叫既有哨表上傳 GAS 的 `guardUpload` action）。

---

## 部署步驟（約 3 分鐘）

### 1. 開啟哨表試算表
Google 雲端硬碟開啟 ID 為 `1sIcdAhw0mz5iM3F5fulDNPOda2pv-t7xUhT6XXf9X7Q` 的試算表（跟哨表上傳工具同一份）。
`哨點設定` 分頁不用手動建，GAS 第一次執行時會自動建立並填好表頭。

### 2. 開啟 Apps Script（新增一個獨立專案，不要跟哨表上傳_GAS_v6.gs 混在同一個專案）
試算表 → 上方選單 **擴充功能 → Apps Script** → 若已有「哨表上傳」專案綁在這份試算表，改用 **左側「+ 新增專案」** 或另外從雲端硬碟建立一個獨立 Apps Script 專案，並用 `SpreadsheetApp.openById(GUARD_SS_ID)` 存取（本檔已這樣寫，不需要綁定在特定試算表上，因此也可以用「script.google.com → 新增專案」直接建立獨立專案）。

### 3. 貼上程式碼
刪掉預設的 `function myFunction(){}`，貼上 `哨表產生_GAS.gs` 的完整程式碼。

### 4. 部署為網頁應用程式
- 右上 **部署 → 新增部署**
- 類型選 **網頁應用程式（Web app）**
- 「執行身分」：**我（你的帳號）**
- 「誰可以存取」：**任何人（Anyone）**
- 按 **部署**，第一次會要求授權（存取早班班表、晚班班表、哨表試算表三份不同試算表）→ 逐一允許

### 5. 複製網址填回工具
- 複製產生的 **`/exec` 結尾網址**
- 把網址給我（Claude Code），我直接寫進 `tool_guard_gen.html` 的 `GUARD_GEN_GAS_URL`
- 個人測試（尚未回填前）：瀏覽器開發者工具直接改 `GUARD_GEN_GAS_URL` 常數值後重新整理

### 6. 之後改 GAS 的鐵則
改任何程式碼後：**部署 → 管理部署 → 編輯（鉛筆）→ 版本：新版本 → 部署**。
**不可**「新增部署」——網址會變，前端全斷。

---

## 資料表結構

**`哨點設定`**（新增分頁，A~B 欄，自動建立）

| A 姓名 | B 適任哨點 |
|---|---|
| 文字 | 逗號分隔字串，例：`警衛室,巡檢1,晚班警衛室` |

> 每次儲存都是整份覆蓋（前端一次把所有員工的勾選狀態送出），不做單筆 upsert，避免半套資料。

早/晚班月班表沿用既有結構（`SCHEDULE_SHEETS_`，跟 `天鷹保全APP_後端_GAS.gs` 完全相同的兩份試算表，本檔獨立部署、獨立維護同一組常數）：B 欄姓名、C 欄起每日一欄班別代號。

---

## API 一覽

| 方法 | action | 參數 | 用途 |
|------|--------|------|------|
| GET | （無） | — | 連線測試，回 `{status:'ok', msg:'天鷹保全 哨表自動產生 API 正常'}` |
| POST | `getEmployeeNames` | — | 讀早/晚班月班表姓名欄，去重合併，回傳排序後陣列 |
| POST | `getGuardPostConfig` | — | 讀「哨點設定」分頁，回傳 `{姓名: [哨點, ...]}` |
| POST | `saveGuardPostConfig` | `configs: [{name, posts:[]}]` | 整份覆蓋寫入「哨點設定」 |
| POST | `getMonthlyRoster` | `year, month, day` | 回傳當天非休假在職名單 `{early:[{name,code}], late:[{name,code}]}` 與國字星期 |

回應統一 JSON 格式 `{status:'ok'|'error', msg?, data?}`。

---

## 驗證清單

- [ ] GAS 部署成功，`/exec` 網址可開啟並回 `{"status":"ok","msg":"天鷹保全 哨表自動產生 API 正常"}`
- [ ] `getEmployeeNames` 回傳的名單跟早/晚班月班表姓名欄實際內容一致（去重）
- [ ] 「哨點設定」分頁勾選後按儲存 → 試算表新增 `哨點設定` 分頁，B 欄為逗號分隔字串
- [ ] 重新整理工具、切回哨點設定分頁 → 之前的勾選狀態正確還原（代表 `getGuardPostConfig` 讀取正確）
- [ ] `getMonthlyRoster` 帶入某天日期 → 回傳名單排除當天休假代號（休/排休/事/病/補/離/支/-/國例）的人
- [ ] 「產生哨表」預覽表格內容跟版型規格逐格核對（尤其巡檢1~7/停巡4~7 同一組陣列、南側門在B欄、見習跳過已佔用列）
- [ ] 「產生 xlsx」下載後用 Excel/Google試算表開啟，合併儲存格範圍正確
- [ ] 「上傳至試算表」成功寫入「今日哨表」或「明日哨表」分頁（沿用既有哨表上傳 GAS，不需重新部署那支）
